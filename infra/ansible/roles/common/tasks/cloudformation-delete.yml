#cf si vider la rds lié

#
- name: delete a stack
  cloudformation:
    stack_name: "{{ TYPENAME }}"
    region: "{{ REGION }}"
    state: "absent"
  tags:
    - clouddelete: "ansible-cloudformation-delete"
  when: GOTODEL == "yes"